This repo contains: two folders, find_exams which contains the code that must be tested and unittests which contains the code to automate the testing of find_exams.py

lectlist.csv and examlist.csv are dample data which is used for testing.

In order to run the tests use the command python3 -m unittest discover from within the ltB folder
